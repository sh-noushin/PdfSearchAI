<!--<Window x:Class="InternalAIAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:InternalAIAssistant"
        Title="AI Assistant" Height="600" Width="400">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <DockPanel>
            <ListBox ItemsSource="{Binding Messages}" DockPanel.Dock="Top" Height="500" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,2,0,2">
                            <TextBlock Text="{Binding Sender}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="10">
                <TextBox Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
                <Button Command="{Binding SendCommand}" Content="Send" Width="70" Margin="5,0,0,0"/>
            </StackPanel>
        </DockPanel>
        --><!-- Busy/thinking overlay --><!--
        <Border Background="#AA000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                CornerRadius="10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="AI is thinking..." Foreground="White" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>-->



<Window x:Class="InternalAIAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:InternalAIAssistant"
        Title="AI Assistant" Height="600" Width="400">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <DockPanel>
            <ListBox ItemsSource="{Binding Messages}" DockPanel.Dock="Top" Height="500" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,2,0,2">
                            <TextBlock Text="{Binding Sender}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="10">
                <TextBox Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
                <Button Command="{Binding SendCommand}" Content="Send" Width="70" Margin="5,0,0,0"/>
            </StackPanel>
        </DockPanel>
        <!-- Busy/thinking overlay -->
        <Border Background="#AA000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                CornerRadius="10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="AI is thinking..." Foreground="White" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>